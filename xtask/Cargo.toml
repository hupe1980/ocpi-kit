[package]
name = "xtask"
version = "0.0.0"
edition = "2024"
publish = false
description = "Repository automation for ocpi-kit: fixture sync and spec coverage."

[dependencies]
regex = "1.13.1"
serde_json = "1.0.151"
